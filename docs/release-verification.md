# Release verification

`tutorials/conditional_detr_detection_colab.ipynb` (`E2E`, **standalone** carrier) is a **release candidate** until the exact notebook revision has executed top-to-bottom in a clean supported runtime. Unit tests, the tiny-model test, JSON validation, code-cell compilation, the generator parity checks and `tools/validate_release_assets.py` are necessary checks but are **not** runtime evidence under DIMER Notebook Specification 2.1 (REL8). This file is the durable release-gate record for the notebook.

The upstream snapshot is pinned to `8f8795fb7c319c7862d4f4cd699e76bb09cf2593` and the notebook is regenerated with that revision and manifest, so it can run. Default-path execution recorded on 2026-09-25 (Kaggle T4; record below). REL12 (step 7, the BYOD dataset exercise) has not been run, so the status stays Candidate.

## Automatic coverage (static and unit, every pull request)

CI installs the pinned CPU-only torch wheel and the other runtime pins, then runs:

- `ruff check src tests tools`;
- `pytest`: snapshot verification and staging against synthetic manifests, the unpinned refusals, the import boundary, input and dataset validation, BYOD directory reading, average precision, the evaluation report, the notebook parity checks, the weight-facts check, and `tests/test_tiny_model.py` — a tiny random-weight Conditional DETR taken through `finetune`, `evaluate`, `save_artifact` and `apply_artifact`;
- `tools/validate_release_assets.py`: model card 1.2 structure and front matter, the pin state across the package, the manifest and the documents, identity consistency, the weight facts, the release-status tokens, and the notebook's structure, carried modules, parity, markers, BYOD gates and location fields;
- `tools/build_notebook.py --check`.

These are source, provenance and unit checks. None of them loads the pinned checkpoint, so none of them is execution evidence.

## Supported release verification procedure

Before changing the registry status from `Candidate` to `Release-grade`:

1. confirm the snapshot is pinned (`MODEL_REVISION` is a 40-hex commit and every manifest entry has a SHA-256) and that static CI is green on the exact commit under review;
2. open that exact notebook revision in a new GPU runtime (Colab or Kaggle, Tesla T4 or similar) with **no repository checkout** and a clean model cache;
3. run the notebook top-to-bottom without editing implementation cells, with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `threshold = 0.7`, `EPOCHS = 10`);
4. verify that Section 1 reports `NOTEBOOK_SOURCE.repository_revision` equal to `metadata.dimer.generated_from.revision`, and that the installed versions equal the inline `PINS`;
5. verify that every default-path stage completes: the pinned install; the carried modules; staging of all four manifest entries and `verify_snapshot`; `validate_inputs` with the `threshold=1.5` rejection finding; COCO detection and the `sample-sanity` report; the blank and noise probes; `validate_dataset`; the split with its leakage assertion; the baseline; `finetune`; the adapted evaluation; new-data inference; adapter export, reload and the tolerance check; and the six outputs in `outputs/`;
6. record the observed baseline and adapted `ap`, `ap50` and `ap75`, the per-epoch losses, the trainable and total parameter counts, the degenerate-probe counts and the wall time in the table below. No metric value is asserted in advance: a low adapted AP is a finding to record, not a failure by itself;
7. exercise the BYOD dataset branch once with a small labelled directory (`BYOD_DATASET_DIR`) and once with an incompatible `annotations.json`, and record both outcomes (REL12);
8. record the notebook Git blob id, commit, runtime (platform, GPU, Python, PyTorch, Transformers, timm), model identifier and revision, whether the model cache was clean, and any warning judged harmless with the reason;
9. record no access tokens or other secrets.

A known-failing default path in the supported runtime blocks release.

## Recorded executions

Notebook identity is the Git blob id of `tutorials/conditional_detr_detection_colab.ipynb` (verify with `git rev-parse <commit>:tutorials/conditional_detr_detection_colab.ipynb`). Each record uses the fields Date, Subject, Runtime, Procedure, Observed result and Caveats.

| Date (UTC) | Subject (commit / notebook blob) | Runtime | Procedure | Observed result | Caveats |
|---|---|---|---|---|---|
| 2026-09-25 | `8824795` / `7204a8c9db12` (notebook generated from `9fd9709`; Section 1 `NOTEBOOK_SOURCE.repository_revision` equals `metadata.dimer.generated_from.revision`; `8824795` differs from `9fd9709` only by the notebook regeneration and a test fix) | Kaggle Tesla T4 (`kurtvalcorza/dimer-nb2-conditional-detr-detection` v1), batch run, image `gcr.io/kaggle-gpu-images/python@sha256:37c64f7dd9c54116ecd1bcc88817c5469b88387388fade02bfa8bf3fc647d461`, driver 580.159.04; Python 3.12.13; after the pinned install: torch 2.14.0+cu130 (CUDA 13.0), Transformers 4.57.6, timm 1.0.29, scipy 1.18.1, numpy 2.5.3, Pillow 11.3.0; model `microsoft/conditional-detr-resnet-50` at `8f8795fb7c319c7862d4f4cd699e76bb09cf2593`; Hugging Face cache clean at start | Steps 1–6 and 8 of the procedure above: exact notebook blob fetched and verified, no repository checkout, every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `threshold = 0.7`, `EPOCHS = 10`). Pass 1 stopped at the install cell's stale-module guard (cuda-bindings loaded 12.9.4 / installed 13.4.3; numpy loaded 2.0.2 / installed 2.5.3), as designed; the runtime was restarted and pass 2 ran from the top. Wall time 301.5 s total (pass 1 210.4 s, pass 2 91.0 s). Step 7 (REL12) not run | 14/14 code cells ok on pass 2. All 4 manifest files staged and verified. `validate_inputs` accepted with the `threshold=1.5` rejection finding. COCO scene at 0.7: 2 detections, `stop sign` 0.871 (box IoU 0.850), `clock` 0.797 (IoU 0.918); `traffic light` and `sports ball` not detected (IoU 0.0); verdict `sample-sanity`, 2/4 drawn objects matched at IoU ≥ 0.5. Probes: blank 0 detections at 0.7 and at 0.05; noise 0 at 0.7, 100 at 0.05 (top 0.117 `orange`, 0.110 `apple`, 0.104 `fire hydrant`). Dataset 40 records / 82 boxes, accepted, no findings; split 30 train / 10 held-out (17 held-out boxes). Baseline `ap` 0.0, `ap50` 0.0, `ap75` 0.0 (0 detections on every held-out image at 0.05). Fine-tune: backbone frozen, 19,940,873 trainable of 43,395,785 parameters, AdamW lr 1e-4, batch 4, float32; epoch losses 1.3464, 1.1591, 1.0539, 0.9122, 0.8195, 0.7461, 0.6693, 0.5863, 0.5601, 0.5423. Adapted `ap` 0.7722468437319924, `ap50` 0.8850646969458852, `ap75` 0.8850646969458852; per-class AP50 `stop-sign` 0.9009900990099009, `yield-sign` 0.7542039918277545, `speed-limit-sign` 1.0. New-data inference (3 images, `NEW_DATA_SEED = 99`, 5 reference signs): **0 detections at `threshold = 0.7` on all 3 images**, every same-label IoU 0.0. Adapter `conditional_detr_adapter.safetensors` 79800004 B, sha256 `16d2d81f8fd2db596391a622fc0324af711951ce52aada2e41b3f54dd72c3ed3`, 323 tensors; reload check 56 detections compared, tolerance 0.001, equivalent. Outputs (sha256): `conditional_detr_detection_annotated.png` `8fab4823b0ff01462d1dc8e0f7546e02f8cf577a2a878b11fe21f79a5c776922`, `conditional_detr_detection_detections.csv` `f96d914442954d232ffc1a817e82bea01d96bc18a4a8329a00ba84981bdf9f63`, `conditional_detr_detection_evaluation_report.json` `37d86ca9baf790bc0957a5d797574e831cb8cd7ce2f3f35b7953f61eddb5460f`, `conditional_detr_detection_input_manifest.json` `578aae9a879f25becf23b337f55f00981a0be9315d63f4390b1e92fc8b1b5aed`, `conditional_detr_detection_result.json` `1de8002c40d61ed535bbb777c7e55b738387605f652131e7b3b610fe9d191413` | REL12 BYOD dataset exercise not run, so this record does not promote the notebook. Held-out AP is one pass over 10 drawn images at the evaluation threshold 0.05, no dispersion estimate. The adapted model found nothing on the 3 unseen images at the default threshold 0.7, recorded as a finding. Harmless warning: Transformers reports `class_labels_classifier.weight`/`.bias` newly initialised because of the shape change (91 → 3 slots); that is the intended head replacement. GPU kernels are not deterministic, so a rerun can differ in the last digits. No tokens or secrets recorded |
