# Release status

Current status: **Candidate — source complete, snapshot pinned, default-path execution recorded on 2026-09-25 (Kaggle T4); REL12 BYOD exercise pending before promotion**. The pipeline package, the offline test suite (including a tiny random-weight Conditional DETR run through fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/conditional_detr_detection_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `8f8795fb7c319c7862d4f4cd699e76bb09cf2593` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `e8553b4fa875…`, matching the Hub's LFS record). A default-path execution with the pinned weights at commit `8824795` (notebook blob `7204a8c9db12`) is recorded in `docs/release-verification.md`: 14/14 code cells completed on a Kaggle Tesla T4 after the documented restart following the install cell. The release gate also requires the REL12 BYOD dataset exercise (a small labelled directory and an incompatible `annotations.json`), which has not been run, so the status stays Candidate.
