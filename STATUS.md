# Release status

Current status: **Candidate — source complete, snapshot pinned, no execution recorded**. The pipeline package, the offline test suite (including a tiny random-weight Conditional DETR run through fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/conditional_detr_detection_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `8f8795fb7c319c7862d4f4cd699e76bb09cf2593` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `e8553b4fa875…`, matching the Hub's LFS record). No execution with the pinned weights is recorded in `docs/release-verification.md`, so the repository is not release-grade.
